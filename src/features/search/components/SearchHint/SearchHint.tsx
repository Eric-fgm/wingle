import React from "react"

export interface SearchHintProps {}

const SearchHint: React.FC<SearchHintProps> = () => {
  return <div className="">SearchHint</div>
}

export default React.memo(SearchHint)
